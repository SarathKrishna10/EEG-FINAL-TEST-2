## Packages
axios | User explicitly requested Axios for API requests if possible, though we will wrap it in React Query for state management.
lucide-react | Standard icon library

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
Backend API is standard Replit Express backend. We will proxy standard /api/* paths.
